function arrays(arr) {
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] = number)

            console.log("valid")
        else
            console.log("invalid")

    }
}

const arr = [1, 2, 3, "str", 4];
console.log(arrays(arr))